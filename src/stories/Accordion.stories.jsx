// Component
import { Accordion } from '../components/Accordion';

const meta = {
    component: Accordion,
};

export const Story = {
    args: {
        expanded: false,
        summary: "Summary of the component",
        details: "Details of the component",
        button_one: "Cancel",
        button_two: "Agree"
    }
};

export default meta;
